apiVersion: v1
kind: Service
metadata:
  name: {{ include "sba.fullname" . }}
  labels:
    {{- include "sba.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "sba.selectorLabels" . | nindent 4 }}
